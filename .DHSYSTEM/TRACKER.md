# Tracker

## Current state

- Current phase: P1 — Bảo vệ figure, scan và formula
- Current task: T-102 — Bảo toàn text trong `LTFigure` và vùng figure
- Status: implementation complete; awaiting Git persistence
- Git persistence: blocked — workspace chưa được khởi tạo Git/upstream.

## Latest verification

- T-102: `python -m unittest discover -s tests -v` passed 68 tests on 2026-08-28.
- T-102 is not marked PASS because the required Git repository and upstream branch are absent.
